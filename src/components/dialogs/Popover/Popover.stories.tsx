import * as React from 'react'
import Popover from './index'
import {Meta, StoryObj} from '@storybook/block'

export default {
  title: '@next/dialogs/Popover',
  component: Popover,
}

type Story = StoryObj<typeof Popover>

export const Basic:Story = {
   args:{

   } 
} 
