import * as React from 'react'
import Tooltip from './index'
import {Meta, StoryObj} from '@storybook/block'

export default {
  title: '@next/dialogs/Tooltip',
  component: Tooltip,
}

type Story = StoryObj<typeof Tooltip>

export const Basic:Story = {
   args:{

   } 
} 
