/**
 *
 * AsyncAutocomplete
 *
 */

import React from 'react'
import cn from 'classnames'
import './async-autocomplete.scss'

const BLOCK = 'autocomplete_async-autocomplete'
function AsyncAutocomplete(props: Props) {
    return <div>AsyncAUtocomplete</div>
}

AsyncAutocomplete.defaultProps = {}

interface Props {}

export default AsyncAUtocomplete
export type { Props as AsyncAUtocompleteProps }
